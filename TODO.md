# TODO: Fix Landing Page and Workflows Error

## Tasks
- [x] Update App.tsx to always render LandingPage on root URL "/"
- [x] Update WorkflowsList.tsx to ensure dataSource is always an array
- [x] Test the application to verify fixes (critical-path: root URL and workflows page load)
